import React from 'react';

const Header = () => (
    <header className="header">
        <h1 className="mainTitle">Pick a color!</h1>
        <h3 className="subTitle">Just tap onto the main color and choose a color you like!</h3>
    </header>
);

export default Header;
